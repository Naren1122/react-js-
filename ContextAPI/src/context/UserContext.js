// this is only for context api example purpose like login context , product context, card context or anything else etc.

import React from "react";

const UserContext = React.createContext();

export default UserContext;
